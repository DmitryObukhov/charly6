/** Simulation field dimensions in pixels. Used by canvas and layout. */
export const FIELD_WIDTH = 800
export const FIELD_HEIGHT = 600

/** Boundary wall thickness in pixels. Must fit at least one brick row/column (BRICK_HEIGHT + BRICK_GAP = 12). */
export const WALL_THICKNESS = 12

/** Cement/mortar color for wall background. Fills gaps so walls are solid and block light. */
export const BRICK_GAP_COLOR = '#3E2723'
